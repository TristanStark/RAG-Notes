# Ce projet envoie les données vers une API externe (RAG backend).
# Si vous souhaitez une BDD locale (fallback) ajoutez ici la création
# d’un SQLite ou TinyDB. Par défaut, no-op.
print("[init] no local DB to create (external API is source of truth)")
